package docker;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

import org.testng.annotations.AfterSuite;
import org.testng.annotations.AfterTest;
import org.testng.annotations.BeforeClass;
import org.testng.annotations.BeforeSuite;
import org.testng.annotations.BeforeTest;

public class Setup_DockerGrid {
	/*
	 * @SuppressWarnings({ })
	 * 
	 * @BeforeTest void startDocker() throws IOException, InterruptedException {
	 * Runtime.getRuntime().exec("cmd /c start start_dockergrid.bat");
	 * Thread.sleep(15000);
	 * System.out.println(" ---------Docker Started----------");
	 * 
	 * }
	 * 
	 * @SuppressWarnings("deprecation")
	 * 
	 * @AfterTest void stopDocker() throws IOException, InterruptedException {
	 * Runtime.getRuntime().exec("cmd /c start stop_dockergrid.bat");
	 * Thread.sleep(15000); System.out.println(" ---------Docker Stoped----------");
	 * }
	 */

}
